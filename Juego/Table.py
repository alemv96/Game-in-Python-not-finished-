#REMINDER: import all the classes to use.
#from Piece import Piece
#better check out for errors on my code.

class Table:
    #Attributes
    CONSTANT
    column, row #both = 10
    bPiece
    cloud
    box
    matrix[[None]]

    def __init__(self):
        self .CONSTANT = 10
        self.column, self.row = CONSTANT , CONSTANT
        countSpaces = CONSTANT * CONSTANT
        counter = 0
        #self.bPiece = BottomPiece("Static")
        for countRow in range(CONSTANT):
            for countColumns in range(CONSTANT):
                counter += 1 

                if counter == countSpaces / 2:
                    matrix[countRow][countColumns] = 
            
    #this method has the responsability to verify the color of every piece, and if two pieces has the same color then destroy both
    #def verifyPieces(bPiece , uPiece): #bottom piece, upper piece

